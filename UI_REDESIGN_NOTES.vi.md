# Aujunpeak VN — ghi chú bản cập nhật giao diện

- Màn hình nhập key đã được rút gọn: chỉ giữ logo, vùng nhập key và các nút thao tác; đã bỏ nhãn `SECURE ACCESS`, tiêu đề kích hoạt và các dòng mô tả phụ.
- Nút kích hoạt dùng tông xanh ngọc/xanh tím, ít đổ bóng và không còn nền chuyển động nặng.
- Nền GIF người dùng gửi được dùng làm nền tĩnh tối ưu cho toàn bộ giao diện. Chọn frame đầu tiên để tránh giật/lag do giải mã GIF liên tục.
- Navigation trên màn hình nhỏ đã đổi thành nút mũi tên ở cạnh trái, mở ra ba icon Trang chủ / Function / Info. Thanh navigation dưới đáy đã bỏ để không che thẻ trạng thái.
- Function dùng icon bánh răng; các thẻ game, thẻ Admin và thẻ Info dùng viền, khoảng cách và nền đồng nhất hơn.
- Các hiệu ứng aurora, blur và các lớp hình trang trí lặp lại đã được bỏ hoặc giảm mạnh.
- Tab Function đã thay header cũ bằng banner GIF Joker có bo góc, chữ `LIVE` xanh và ảnh chuyển động đầy đủ.
- Các switch khi bật dùng màu đỏ; banner nền app cũng được đưa vào bundle dưới dạng GIF để tiếp tục chuyển động trên các tab.
- Thêm lớp kiểm tra key khi mở app: hiển thị đang kiểm tra, sau đó báo key đã xác thực hoặc chuyển sang màn hình nhập key.